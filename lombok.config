# Lombok configuration for ZombieZ
# This file configures Lombok annotation processing

# Enable builder copy method
lombok.builder.className=*Builder

# Ensure all Lombok features are enabled
lombok.anyConstructor.addConstructorProperties=true

# Suppress warnings for field access
lombok.addLombokGeneratedAnnotation=true
